1. будет выводиться undefind. Т.к. в объекте window в данном примере всегда 'a'
и в условие if мы не зайдем
2. не понял я, почему а не определена
3. alert возращает ссылку на функцию a, т.е. ввыдет функцию
4. выведет число 10, т.к. мы заменям третий по счету, ну или элемент с индексом 2
5. возращает объект window
